# I18n

## Objectif

Preparer la traduction en francais puis dans d'autres langues sans changer le moteur de jeu.

La V1 narrative utilise seulement `en`, mais le shell UI est deja disponible en `en` et `fr`.

Le modele est separe :

- graphe logique ;
- messages narratifs affiches ;
- messages UI.

## Principe

La logique ne doit jamais etre traduite.

Ne pas traduire ou modifier :

- scene IDs ;
- targets ;
- variables ;
- conditions ;
- assignments ;
- special values ;
- achievement variables ;
- ordre logique des blocs.

Traduire seulement :

- paragraphes ;
- choix ;
- titres/captions d'achievements ;
- UI shell.

## Format Actuel

Locale anglaise :

```text
content/canonical/v1/locales/en/<chapterId>.json
content/canonical/v1/locales/en/achievements.json
content/canonical/v1/locales/en/ui.json
content/canonical/v1/locales/fr/ui.json
```

Forme :

```json
{
  "locale": "en",
  "chapterId": "ch1",
  "messages": {
    "ch1.Ch1_Intro1.p1": "They say there is...",
    "ch1.Ch1_Intro1.c1": "Excited"
  }
}
```

Le graphe reference uniquement les `messageId`.

Les fichiers UI viennent de :

```text
content/ui-locales/en.json
content/ui-locales/fr.json
```

Ils sont packs en runtime sous les clés `locales/en/ui` et `locales/fr/ui`. `index.uiLocales` liste les locales UI disponibles.

## Langue UI Et Langue Narrative

`settings.uiLocale` pilote seulement le shell d'interface : navigation, panneaux, settings, about, notices et aides.

`GameState.locale` reste la langue narrative du contenu. Pour l'instant, les paragraphes, choix, stat labels et achievements restent en anglais.

Changer l'interface FR/EN ne doit pas reset la partie, ne doit pas modifier l'historique, et ne doit pas tenter de charger des chapitres narratifs français.

## Ajouter Une Langue

Future procedure attendue :

1. generer un squelette de locale depuis `en` ;
2. conserver exactement les memes `messageId` ;
3. remplir les valeurs traduites ;
4. valider la couverture ;
5. generer les runtime packs de la locale.

Chemin cible :

```text
content/canonical/v1/locales/fr/<chapterId>.json
```

Puis pack runtime :

```text
src/generated/packs/locales__fr__<chapterId>.ts
```

## Fallback

Le runtime actuel tombe sur `en` si un pack locale n'existe pas.

Pour une vraie locale future, ajouter une validation stricte :

- toutes les clés `en` doivent exister dans la locale ;
- pas de clés en plus sauf convention explicite ;
- rapport de couverture par chapitre ;
- test que le changement de langue conserve l'etat joueur.

## Export Traduction

Format d'echange recommande pour la suite :

- JSON plat pour iteration rapide ;
- XLIFF pour workflow traduction plus professionnel.

Le format runtime doit rester les packs generes, pas XLIFF.

## UI Shell

Les libelles UI sont dans des bundles source :

```text
content/ui-locales/en.json
content/ui-locales/fr.json
```

Ils sont generes puis charges comme packs runtime :

- `locales/<locale>/ui`
- fallback `locales/en/ui`

Cela concerne :

- Saves ;
- Abilities ;
- Achievements ;
- Settings ;
- About ;
- notices d'erreur ;
- labels export/import.

`content:validate` verifie que les locales UI ont exactement les memes cles que `en` et que les packs correspondants existent.

Le bloc About visible dans l'UI contient l'attribution officielle de cette adaptation :

```text
Based on “Magium” by Cristian Mihailescu / Chris Michael Wilson.
Original story/data licensed under Creative Commons Attribution 4.0 International (CC BY 4.0).
French translation and adaptation by Foobar, 2026.
This is an unofficial adaptation and is not endorsed by the original author’s estate or the Magium community maintainers.
Source: https://github.com/raduprv/Magium
License: https://creativecommons.org/licenses/by/4.0/
Changes: French translation, UI adaptation, technical restructuring, additional interface features.
```
